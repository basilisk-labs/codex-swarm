# T-070: agentctl guard commit: add --auto-allow

## Goal

- Remove an extra command from the commit workflow by letting `guard commit` infer `--allow` prefixes automatically.

## Scope

- `scripts/agentctl.py`:
  - Extend `guard commit` with `--auto-allow` (same behavior as the existing `commit --auto-allow` wrapper).
  - Keep current behavior when explicit `--allow` is provided.
- Docs:
  - Update `.codex-swarm/agentctl.md` to recommend `guard commit --auto-allow` when `--allow` is not already known.

## Verification

- `python -m compileall scripts/agentctl.py`
- `python scripts/agentctl.py task lint`
- Manual smoke:
  - Stage a small change and run `python scripts/agentctl.py guard commit T-### -m \"...\" --auto-allow` and confirm it passes without explicit `--allow`.
