# Architecture & Workflow

The full architecture/workflow documentation lives in `README.md` under the **Architecture & Workflow** section (kept there for easier discovery).

This file remains as a stable pointer for links and for the “project docs live in `docs/`” convention.

